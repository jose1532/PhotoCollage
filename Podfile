# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoGrid' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5.2'
  pod 'Kingfisher', '~> 6.0'
  pod 'ProgressHUD'

  # Pods for PhotoGrid

  target 'PhotoGridTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoGridUITests' do
    # Pods for testing
  end

end
